import type { JSX } from "preact";

export const frontmatter = {
  title: "Valid Page",
  description: "A valid TSX page",
};

export default function ValidPage(): JSX.Element {
  return <h1>Valid Page</h1>;
}
